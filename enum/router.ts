export enum Routes {
    Student = "(student)/(tabs)",
    Teacher = "(teacher)/(tabs)",
    Admin = "(admin)/(tabs)",
    Parent = "(parents)/(tabs)",
    Default = "choosing",
    Login = "/(modals)/login",
}